import React from 'react';
import Icon from 'oxclib/Icon';

/** Code example for Icon: user-circle, light, 2x */
export default () => {
  return <Icon name='user-circle' size={'2x'} light={true} />;
};
